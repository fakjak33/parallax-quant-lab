# Copy this file to `secrets.toml` (same folder) and set a strong password.
# `secrets.toml` is gitignored and must NEVER be committed.
#
# On Streamlit Community Cloud, paste this same line into:
#   App → Settings → Secrets
app_password = "change-me-to-a-long-random-passphrase"
